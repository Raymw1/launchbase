const idade = 19;

// if (!(idade >= 18) && idade === 17) {
if (!(idade >= 18) || idade === 17) {
    console.log("Entrada bloqueada!")
}   else {
    console.log("Ela já tem 18 anos. Ela pode entrar")
}